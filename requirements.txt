# Runtime dependencies for the Cloud Run function (entry point: Process).
# Slimmed from a full-environment freeze to the packages the request path actually imports
# (main.py -> localpackage: game/basePerson/curve/dataSet/SAR/utils/spa/TablesAD/calcs/person).
# This removes the FastAPI / Jupyter / build-tool bloat that caused build dependency conflicts.
# Versions match the tested Python 3.11 environment (18/18 Additional-Tables regression passes).
numpy~=1.24.2
pandas~=1.5.3
scipy~=1.10.1
matplotlib~=3.7.1
seaborn~=0.12.2
pymaybe~=0.1.6
python-dateutil~=2.8.2
